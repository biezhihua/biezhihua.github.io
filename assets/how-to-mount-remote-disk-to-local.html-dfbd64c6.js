import{_ as s,Y as a,Z as e,a2 as n}from"./framework-301d0703.js";const i={},o=n(`<h1 id="在ubuntu中挂载远程文件系统到本地" tabindex="-1"><a class="header-anchor" href="#在ubuntu中挂载远程文件系统到本地" aria-hidden="true">#</a> 在ubuntu中挂载远程文件系统到本地</h1><p>在Ubuntu中挂载远程文件系统到本地通常涉及使用网络文件系统（NFS）、Samba共享或SSH文件系统（SSHFS）。每种方法都适用于不同的场景。下面是如何使用这些方法中的每一种来挂载远程文件系统的步骤。</p><h3 id="使用sshfs挂载" tabindex="-1"><a class="header-anchor" href="#使用sshfs挂载" aria-hidden="true">#</a> 使用SSHFS挂载</h3><p>SSHFS（SSH Filesystem）允许你通过SSH协议安全地挂载远程文件系统。这是一种安全且相对简单的方法，特别适用于已经设置了SSH访问的情况。</p><ol><li><p><strong>安装SSHFS</strong>:</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code><span class="token function">sudo</span> <span class="token function">apt-get</span> update
<span class="token function">sudo</span> <span class="token function">apt-get</span> <span class="token function">install</span> sshfs
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div></div></div></li><li><p><strong>创建本地挂载点</strong>:</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code><span class="token function">mkdir</span> ~/remoteDir
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div></li><li><p><strong>挂载远程文件系统</strong>:</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code>sshfs user@hostname:/remote/directory/path ~/remoteDir
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>其中，<code>user</code>是远程系统上的用户名，<code>hostname</code>是远程系统的主机名或IP地址，<code>/remote/directory/path</code>是远程系统上你想要挂载的目录的路径，<code>~/remoteDir</code>是本地挂载点。</p></li><li><p><strong>卸载远程文件系统</strong>: 当不再需要时，可以使用以下命令来卸载远程文件系统：</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code>fusermount <span class="token parameter variable">-u</span> ~/remoteDir
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div></li></ol><h3 id="使用nfs挂载" tabindex="-1"><a class="header-anchor" href="#使用nfs挂载" aria-hidden="true">#</a> 使用NFS挂载</h3><p>NFS（网络文件系统）是另一种广泛使用的方法，特别适用于Linux或UNIX系统之间的文件共享。</p><ol><li><p><strong>确保NFS客户端已安装</strong>:</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code><span class="token function">sudo</span> <span class="token function">apt-get</span> update
<span class="token function">sudo</span> <span class="token function">apt-get</span> <span class="token function">install</span> nfs-common
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div></div></div></li><li><p><strong>创建本地挂载点</strong>:</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code><span class="token function">mkdir</span> ~/nfsMount
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div></li><li><p><strong>挂载NFS共享</strong>:</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code><span class="token function">sudo</span> <span class="token function">mount</span> <span class="token parameter variable">-t</span> nfs hostname:/exported/path ~/nfsMount
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>其中，<code>hostname</code>是NFS服务器的主机名或IP地址，<code>/exported/path</code>是NFS服务器上已导出的目录的路径，<code>~/nfsMount</code>是本地挂载点。</p></li></ol><h3 id="使用samba挂载" tabindex="-1"><a class="header-anchor" href="#使用samba挂载" aria-hidden="true">#</a> 使用Samba挂载</h3><p>Samba是在Windows和Linux/UNIX系统之间共享文件和打印服务的标准。</p><ol><li><p><strong>安装cifs-utils</strong>:</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code><span class="token function">sudo</span> <span class="token function">apt-get</span> update
<span class="token function">sudo</span> <span class="token function">apt-get</span> <span class="token function">install</span> cifs-utils
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div></div></div></li><li><p><strong>创建本地挂载点</strong>:</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code><span class="token function">mkdir</span> ~/sambaShare
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div></li><li><p><strong>挂载Samba共享</strong>:</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code><span class="token function">sudo</span> <span class="token function">mount</span> <span class="token parameter variable">-t</span> cifs <span class="token parameter variable">-o</span> <span class="token assign-left variable">username</span><span class="token operator">=</span>sambaUser,password<span class="token operator">=</span>sambaPassword //hostname/sharename ~/sambaShare
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>其中，<code>username</code>和<code>password</code>是访问Samba共享所需的凭据，<code>hostname</code>是Samba服务器的主机名或IP地址，<code>sharename</code>是共享的名称，<code>~/sambaShare</code>是本地挂载点。</p></li></ol><p>在使用任何挂载方法之前，请确保你有权访问远程文件系统，并且远程系统已正确配置以允许远程访问。对于NFS和Samba，这可能涉及到在服务器端进行特定的配置。对于SSHFS，你只需要在远程系统上有一个有效的SSH账户。</p>`,12),d=[o];function t(l,r){return a(),e("div",null,d)}const p=s(i,[["render",t],["__file","how-to-mount-remote-disk-to-local.html.vue"]]);export{p as default};
