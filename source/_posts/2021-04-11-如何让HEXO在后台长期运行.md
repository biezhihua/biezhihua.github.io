---
title: 如何让HEXO在后台长期运行?
date: 2021-04-11 11:55:57
tags:
---


# NOHUP自动终止

在使用`nohup`的时候，一般都配合`$`使用，但是在实际使用过程中，很多挂在后台就不管了，在这种情况下，当前账户非正常退出或者结束的时候，命令还是自己结束了。

所以在使用`nohup`命令后台运行命令之后，需要按回车，然后退回到Shell输入命令窗口，使用`exit`正常退出当前账户，而不是每次在`nohup`执行成功后直接关闭终端。

这样才能保证命令一直在后台运行。

# 使用PM2让HEXO长期在后台运行

pm2可以让一个应用长期后台运行。

安装pm2:
{% codeblock lang:javascript %}
npm  install -g pm2
{% endcodeblock %}

在博客根目录下面创建一个hexo_run.js:
{% codeblock lang:javascript %}
const { exec } = require('child_process')
exec('hexo server',(error, stdout, stderr) => {
        if(error){
                console.log('exec error: ${error}')
                return
        }
        console.log('stdout: ${stdout}');
        console.log('stderr: ${stderr}');
})
{% endcodeblock %}

# 引用

- https://github.com/Unitech/pm2
- https://cloud.tencent.com/developer/article/1147161
- https://blog.csdn.net/tangcuyuha/article/details/80331169 
- https://github.com/theme-next/hexo-theme-next
- https://www.runoob.com/linux/linux-comm-nohup.html